import type { Meta } from '@storybook/react'; import { Calendar } from './Calendar';
export default { title: 'omni-ui-components/Calendar', component: Calendar } satisfies Meta<typeof Calendar>;
export const Default = { args: {} };
